name: "Vinyl Guardian"
description: "Listens to ADC line-in, fingerprints audio via AcoustID, and updates HA via MQTT."
version: "1.0.5"
slug: "vinyl_guardian"
init: false
arch:
  - aarch64
  - amd64
  - armv7
audio: true
startup: application
boot: auto
# This schema creates the Configuration UI boxes in Home Assistant
schema:
  acoustid_key: str
  mqtt_broker: str
  mqtt_port: int
  mqtt_user: str?
  mqtt_password: password?
  audio_threshold: float
  debug_logging: bool
# These are the default values shown to users
options:
  acoustid_key: ""
  mqtt_broker: "192.168.X.X"
  mqtt_port: 1883
  mqtt_user: ""
  mqtt_password: ""
  audio_threshold: 0.015
  debug_logging: true
